;; string-replaceAll function replaces all the "old" occurances in the "original"
;; string with "new"
(fn string-replaceAll |original old new|
    (string-replace original old new -1)
    )

;; string-count is used to count the number of occurances of "sub" in the "str"
(fn string-count |str sub|
    ()
    )
